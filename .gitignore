# 1. Ignore all macOS debugging symbol folders anywhere in the workspace
**/*.dSYM/

# 2. Ignore all compiled extensionless binary executables globally
# (This matches files that do not have a dot in their filename)
*
!*/
!*.*

# 3. Explicitly allow the files you actually care about tracking
!*.cpp
!*.md
!.gitignore
!LICENSE